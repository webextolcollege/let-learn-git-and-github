const f_Num=7;
//declare and initaiize variable for first num
const l_Num=5;
//declare and initaiize variable for second num
//calculate multiplication of given two numbers
const mul=f_Num*l_Num;
//display the multiplication of two numbers
console.log('The multiplication of '+f_Num+' and '+l_Num+' is: '+mul);